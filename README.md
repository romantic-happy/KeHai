# KeHai
## 启动项目过程
1. 启动数据库服务，确保与后端代码配置文件src/config/config.local.ts里的数据库配置一致
2. 在后端根目录下启动后端服务
```bash
$ npm install(首次启动需运行)
$ npm run dev
```
3. 在前端根目录下启动前端服务
```bash
$ npm install(首次启动需运行)
$ npm run dev
```
4. 即可在浏览器中预览网站 [http://localhost:9000/](http://localhost:9000)   
初始账户：admin，密码：123456
